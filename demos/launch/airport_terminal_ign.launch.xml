<?xml version='1.0' ?>

<launch>

  <!-- Common launch -->
  <include file="$(find-pkg-share demos)/common.launch.xml">
    <arg name="use_sim_time" value="true"/>
    <arg name="viz_config_file" value ="$(find-pkg-share demos)/include/airport_terminal/airport_terminal.rviz"/>
    <arg name="config_file" value="$(find-pkg-share rmf_demo_maps)/airport_terminal/airport_terminal.building.yaml"/>
  </include>

  <group>
    <let name="world_path" value="$(find-pkg-share rmf_demo_maps)/maps/airport_terminal_ign/airport_terminal.world" />
    <let name="model_path" value="$(find-pkg-share rmf_demo_maps)/maps/airport_terminal_ign/models:$(find-pkg-share rmf_demo_assets)/models" />
    <let name="resource_path" value="$(find-pkg-share rmf_demo_assets)/media:$(find-pkg-share rmf_demo_assets)" />
    <let name="plugin_path" value="$(find-pkg-prefix building_sim_ign)/lib" />
    <executable cmd="ign gazebo -r -v 4 $(var world_path)" output="both">

      <env
        name="IGN_FILE_PATH"
        value="$(var model_path):$(var world_path)" />

      <env
        name="SDF_PATH"
        value="$(var model_path):$(var world_path)" />

      <env
        name="IGN_GAZEBO_SYSTEM_PLUGIN_PATH"
        value="$(var plugin_path)"/>

    </executable>
  </group>

</launch>
